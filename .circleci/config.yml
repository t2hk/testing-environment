version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.9.5
    steps:
      - checkout

      - run: |
           pwd
           ls -la ~/

      - restore_cache:
          keys:
            - deps-{{ checksum "~/project/work/calcdemo/poetry.lock" }}

      - run:
          working_directory: ~/project/work/calcdemo/
          name: Install Dependencies
          command: poetry install

      - save_cache:
          key: deps-{{ checksum "~/project/work/calcdemo/poetry.lock" }}
          paths:
            - ~/project/work/calcdemo/

      - run:
          name: Exec pytest
          command: |
            poetry run pytest
            source $(find . -name circleci-poetry*)/bin/activate
            pytest
